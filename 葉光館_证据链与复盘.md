# 葉光館 · 证据链与复盘（内部准备版）

> 用途：面试前把「已上线」变成「有统计口径、有真实反馈、有复盘」的证据链。
> 原则：没有证据的数字不写；写了就必须能定位到文件、URL 或一手来源。

## 1. 当前证据状态

| 证据 | 现状 | 缺口 |
| --- | --- | --- |
| PRD | `02_yeguang_PRD.md` 存在，版本 v5.2 | 作品数写 1,307，与实际库不一致 |
| 高保真原型 | `01_yeguang_prototype.html` 存在，13 页 | 未附线上 URL / 统计 / 反馈入口 |
| 线上站点 | 有 Worker 部署链接，但访问可用性需复核 | 未确认当前是否在线 |
| 真源码 | Next.js 源码存在，含 `photos.json` 3,358 条 | README 元信息漂移，写的是「小葉摄影」 |
| 访问统计 | 无 UV/PV/事件统计 | 需要 Cloudflare Web Analytics + 自建事件接口 |
| 用户反馈 | 无一手问卷/访谈/录屏 | 需要 5-10 位画师/创作用户 |
| 复盘 | 内部复盘存在，数据停在 622 张 | 未进入作品集，数据需更新 |
| 数据口径 | 当前实际 3,358 条：original 445、pixiv 2,206、pixiv_monthly 707 | 主站 2700+、PRD 1,307、复盘 622 全部不一致 |

## 2. 数据口径统一方案

对外只使用一条权威口径，其余都标「旧口径」：

- 收录数：以真源码 `photos.json` 为准，当前 3,358 条。
- 来源：Pixiv 日榜/月榜/收藏集。
- 展示格式：`收录 3,358 件 · 最近同步日期 · 来源：Pixiv 日榜/月榜/收藏集`。
- 统计区间：必须写明起止日期、域名、剔除规则，不把估算当真实用户数。

## 3. 访问统计方案

### 3.1 基础 UV/PV

在页面 HTML 头部接入 Cloudflare Web Analytics beacon，或在 Cloudflare Dashboard 开启 Web Analytics。

- PV：JS beacon 成功加载的 HTML 页面数。
- UV：同一匿名 visitorId 同一天只计一次。
- 数据来源：Cloudflare 自带来源、设备、地区，不需要额外存储。

### 3.2 自定义事件

在现有 Worker 增加 `POST /__stats/event` 路由，返回 204，单条请求限制 1KB，只接受白名单事件名：

- `lightbox_open`
- `filter_click`
- `submit_start`
- `submit_success`
- `theme_change`

存储建议用 Cloudflare Workers KV：

```text
stats:YYYY-MM-DD:uv:{visitorId}
stats:YYYY-MM-DD:pv
stats:YYYY-MM-DD:{event}
```

### 3.3 隐私与口径

- 不存 IP、不存 Cookie、不采集作品标题/画师名/PII。
- 浏览器生成随机 visitorId 写 `localStorage`，按天哈希后入库。
- 提供退出开关：`localStorage.setItem('leafiy_stats_off','1')`。
- 明确排除本人、开发环境、爬虫和预取流量。
- 对外展示统一写「匿名统计，仅用于项目验证」。

## 4. 真实反馈采集模板

开场说明：这份问卷用于改进一个面向画师/创作者的个人作品展示站，约需 5 分钟。回答会匿名整理，不会公开你的联系方式。

1. 你目前的创作频率是？A 每周至少 3 次，B 每周 1-2 次，C 每月几次，D 很少创作。
2. 你主要在哪些平台发布或展示作品？可多选：Pixiv、微博、B 站、小红书、Lofter、其他。
3. 你目前有没有独立作品站或主页？A 没有，B 有但很少更新，C 有并持续更新。
4. 如果有一个「零代码、自动同步、无广告」的个人展馆，你最想用它做什么？
5. 请打开葉光館浏览 2 分钟：第一眼最吸引你的地方是什么？
6. 同样浏览后，第一眼最不吸引你的地方是什么？
7. 你能否在 3 分钟内找到「日榜」和「月榜」，并打开任意一张作品？A 很顺利，B 有点犹豫，C 找不到。
8. 浏览后，你对作品来源和画师标注是否清楚？A 清楚，B 部分清楚，C 不清楚，D 没注意。
9. 你是否愿意把葉光館当成自己的个人展馆？A 愿意，B 看功能，C 不愿意。
10. 什么功能最影响你决定用还是不用？
11. 如果站内展示「最近同步时间、作品数量和来源说明」，你会更信任它吗？A 会，B 无所谓，C 不会。
12. 你愿意后续参加一次 15 分钟录屏访谈吗？如果愿意，请留下联系方式。

整理表字段：

`respondent_id, filled_at, channel, creative_frequency, platforms, has_site, use_case, first_positive, first_negative, task_success, source_clarity, willingness, decision_factor, trust_impact, interview_consent, contact, raw_note, evidence_level, can_quote`

`evidence_level` 固定写「一手访谈」或「二手转述」，面试引用只采信一手。

## 5. 复盘大纲

1. 结论摘要：项目一句话、当前验证结论、证据等级。
2. 证据清单：PRD、原型、线上站点、统计、反馈、录屏各一条，附来源。
3. 用户与假设：原假设、已验证、未验证。
4. 数据口径：统计区间、PV/UV 定义、剔除规则、作品数来源。
5. 反馈与问题：5-10 人汇总、原话、问题优先级。
6. 迭代与行动：因反馈改了什么、预期指标、下一步。
7. 风险与边界：版权、隐私、国内访问、数据漂移。

验收标准：一页正文 1000-1200 字加证据表；没有证据的数字不写，写了就有点位。

## 6. 最小功能建议

1. P1：页脚/About 加「收录 3,358 件 · 最近同步日期 · 来源」，让自动同步可审计。
2. P1/P2：页脚加「给展馆提建议」入口，链接到飞书表单或邮件模板。
3. P2/P3：最近同步批次加很小的「新」标记，用现有 `date` 字段，不新增后端。

## 7. 本轮验收清单

- [ ] 已建立独立证据链文档，不修改真源码。
- [ ] 数据口径已列明 3,358 与旧口径差异。
- [ ] 统计方案已明确事件、存储、隐私。
- [ ] 反馈模板可复制使用。
- [ ] 复盘大纲已可逐项填写。
