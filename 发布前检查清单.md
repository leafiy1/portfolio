# 作品集 push / 上线前检查清单

> 规则：未经用户明确同意，禁止执行 `git push`；`push` 不等于“上线”，上线必须单独确认部署方式。
> 用途：用户确认 push 后，按这份清单执行，避免公开 Pages 时把未验证或不想公开的内容一起推上去。

## A. 用户确认

- [ ] 用户明确说「可以 push」。
- [ ] 用户确认是否接受 GitHub Pages 公开可见；若不想公开，先在 GitHub 设置关闭 Pages 或改用非 Pages 分支。
- [ ] 确认本次要发布的文件范围。

## B. 数据与页面验证

- [ ] `node 05_dungeon_meshi_game/verify_game.cjs`：10/10 PASS。
- [ ] `node gear/verify_gear.cjs`：10/10 PASS，控制台 0 错误。
- [ ] `products.json` 总数、分类数、ID 唯一性检查通过。
- [ ] 主站 GearPick 数字为 3070 款 / 7 品类，无 2396 / 4 品类残留。
- [ ] 简历 HTML/DOCX/PDF 使用 3070 / v6.0。
- [ ] 本地 HTTP 预览主站、GearPick、迷宫饭均能打开。

## C. Git 操作

- [ ] `git status` 确认没有意外文件。
- [ ] `git diff --check` 通过。
- [ ] 提交信息写清本轮内容（例如 GearPick v6.0 + 迷宫饭 v2.5.0）。
- [ ] 提交后再次确认本地分支与要推的远端分支正确。
- [ ] 执行 `git push`。

## D. 上线后核验

- [ ] 访问 `https://leafiy1.github.io/portfolio/`，确认新版已发布。
- [ ] 访问 GearPick 和迷宫饭两个子页面，确认公开链接可打开。
- [ ] 控制台抽样检查 0 错误；移动端无横向溢出。
- [ ] 更新 CHECKPOINT 和交接卡为「已 push / 已上线」状态。

## E. 失败回退

- 如果 push 后公开页面异常，先判断是缓存还是部署失败；不要直接回滚历史提交。
- 如需回退，先向用户说明影响并单独确认，再操作。
